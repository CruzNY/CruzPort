{% extends 'base.html' %}
{% block title%}CruzPort | {{ details.title }} {% endblock %}
{% block content %}
<section class="section">
  <div class="container">
    <div class="content">
      <h1 class="title">{{ details.title }}</h1>
      <h2 class="subtitle">By {{ details.authors }} </h2>
      <h2 class="subtitle is-6">Submitted {{ details.submission_date }}</h2>
      <figure class="image">
	<img alt="" src="{{ details.image.url }}"/>
      </figure>
      <p>{{ details.full_body }}</p>
    </div>
  </div>
  
</section>

{% endblock %}
